import { ConfigFile } from "../types/other-types"


let configFile: ConfigFile | null = null;


/**
 * returns the configuration file (if it exists)
 */
export const getConfig = (): ConfigFile | null => {
  return null;
}

export const saveConfig = (config: ConfigFile): void => {
  // 
}
